Toy harness used to learn more about how agents work. Also, it's an experiment: manually
write just enough code to call an OpenAI-compatible Chat Completions endpoint in a loop
and then work with a language model to improve the harness itself. No vibe-coding
though, the aim here is to understand how agent loops work.
